name: release

on:
  release:
    types: [created]


jobs:
  release:
    name: "release"
    runs-on: "ubuntu-latest"

    steps:
      - name: "Build"
        run: |
          make

      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          prerelease: false
          title: "Release"
          files: |
            LICENSE.txt
            apple_set_os_loader.efi
            
  
